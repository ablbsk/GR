import styled from "styled-components";
import { FormButton, FormInput } from "../../../style-constants";

export const FormField = styled.div`
  margin: 2em 0;
  text-align: center;
`;

export const Input = FormInput;

export const Button = FormButton;
